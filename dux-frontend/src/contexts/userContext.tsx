import { createContext } from "react";
{// origin folder of the context, go to top section of App for definition of this context
    }
const UserContext = createContext(null);
export default UserContext;